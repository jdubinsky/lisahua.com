import { h, Component } from "preact";

export default class MagnentCaseStudyPage extends Component {
    render() {
        return (
            <div>
                Magnent Forensics
            </div>
        );
    }
}